# =============================================================================
# Open Search Service - Environment Configuration
# =============================================================================

# Database Configuration
DATABASE_URL=postgresql://search_user:search_password@localhost:5432/search_db
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20

# Vector Backend Configuration
# Options: pgvector (recommended), faiss
VECTOR_BACKEND=pgvector

# Embedding Provider Configuration
# Options: local (recommended), openai
EMBEDDING_PROVIDER=local

# Local Embedding Model Configuration
EMBED_MODEL=all-mpnet-base-v2
EMBED_DIM=768

# OpenAI Configuration (only needed if EMBEDDING_PROVIDER=openai)
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=text-embedding-3-small

# Search Configuration
TOP_K=5
CHUNK_TOKENS=512

# Reranker Configuration
RERANKER_ENABLED=true
RERANKER_MODEL=cross-encoder/ms-marco-MiniLM-L-6-v2

# FAISS Configuration (only needed if VECTOR_BACKEND=faiss)
FAISS_INDEX_PATH=/data/faiss
FAISS_M=32
FAISS_EF_CONSTRUCTION=200
FAISS_EF_SEARCH=64

# Logging Configuration
LOG_LEVEL=INFO

# =============================================================================
# Quick Start Configurations
# =============================================================================

# # Production with pgvector (recommended)
# VECTOR_BACKEND=pgvector
# EMBEDDING_PROVIDER=local
# RERANKER_ENABLED=true
# TOP_K=10

# # High-performance with OpenAI
# VECTOR_BACKEND=pgvector
# EMBEDDING_PROVIDER=openai
# OPENAI_API_KEY=sk-your-key-here
# RERANKER_ENABLED=true

# # Development with FAISS
# VECTOR_BACKEND=faiss
# EMBEDDING_PROVIDER=local
# RERANKER_ENABLED=false

# =============================================================================
# Notes:
# - Copy this file to .env and customize values
# - pgvector is recommended for production use
# - Local embeddings are free but require ~2GB RAM
# - OpenAI embeddings are paid but require less local resources
# =============================================================================
